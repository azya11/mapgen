# Heavy 3D-pipeline dependencies (CLI + generation worker + full local dev).
# Intentionally NOT installed on Vercel — these exceed the serverless bundle
# limit and the generation work runs on a separate always-on worker instead.
numpy>=1.26
trimesh>=4.4
shapely>=2.0
mapbox-earcut>=1.0
scipy>=1.11
networkx>=3.0
requests>=2.31
anthropic>=0.40
